<header class="header">
    <div class="logo-box">
        <img class = "onionheader" src="images\theonionbackground.jpg" alt="The Onion">
    </div>
    <div class="text-box">
        <h1 class="heading-primary">
          <span class="heading-primary-sub">Press button below</span>
        </h1>
        <a id="scrape" class="btn btn-white btn-animated" href="{{{route}}}">{{btn}}</a>
    </div>
</header>
<div class="container">
    <div class="card-deck mt-3">
        <div class="message text-center">
          <h2>{{message}}</h2>
        </div>
    </div>
</div>